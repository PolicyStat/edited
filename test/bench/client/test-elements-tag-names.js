// the `tagNames` of the elements we test on.
module.exports = [
  'div', // block
  'p', // block
  'span', // inline
  'textarea', // already editable inline-block
  'input' // already editable inline-block
]
